Artist Tracker — Code Export
=============================
Scripts:        scripts/
Config template: config/config.env.template  (fill in your own API keys)
Upload server:  upload_server.py  (port 8765)
Handles tool:   handles_tool.py   (port 8766)

NOTE: config.env.template has all sensitive values redacted.
      Copy to config/config.env and fill in your keys before running.

Database and Airtable base not included in this export.
